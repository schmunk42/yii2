version: '2'

services:

  php:
    depends_on:
      - postgres

  postgres:
    image: postgres
    environment:
      - POSTGRES_DB=yiitest
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres